<Window x:Class="TestControllerManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:viewModel="clr-namespace:TestControllerManager.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewModel:MainWindowViewModelPreview, IsDesignTimeCreatable=True}"
        Title="MainWindow" Height="350" Width="600"
        >
    <Window.DataContext><viewModel:MainViewModel ></viewModel:MainViewModel></Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Name="ControlAreaGrid" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="500" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Name="FooLabel" Grid.Column="0" Content="{Binding Path=Foo}" />
            <TextBox Name="BarText" Grid.Column="1" IsEnabled="True" Text="{Binding Path=Bar}" />
        </Grid>
    </Grid>
</Window>
